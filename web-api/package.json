{
  "name": "hyleria-api",
  "version": "1.0.0",
  "description": "Fetch user data via a web-exposed RESTful API",
  "main": "server.js",
  "scripts": {
    "start": "nodemon server.js --exec babel-node --presets es2015",
    "test": "mocha --compilers js:babel-core/register --timeout 10000"
  },
  "author": "Ben (OutdatedVersion)",
  "devDependencies": {
    "babel-cli": "^6.23.0",
    "babel-core": "^6.23.1",
    "babel-preset-es2015": "^6.22.0",
    "babel-register": "^6.23.0",
    "hippie": "^0.5.1",
    "mocha": "^3.2.0"
  },
  "dependencies": {
    "restify": "^4.3.0"
  }
}
